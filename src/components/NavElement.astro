---
import "../styles/index.css";

const { name, href, isSelected } = Astro.props;
---

<a href={href}>
  <div
    class=`${isSelected ? "font-bold": ""} text-(--dark-blue) 
    hover:text-(--light-gray)`
  >
    {name}
  </div>
</a>
