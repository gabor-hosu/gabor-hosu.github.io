---
import { Icon } from "astro-icon/components";
import "../styles/index.css";

const { class: className, name, url } = Astro.props;
---

<a href={url}>
  <Icon name={name} class=`m-2 size-6 ${className}` />
</a>
